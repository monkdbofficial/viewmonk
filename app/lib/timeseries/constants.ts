// ─────────────────────────────────────────────────────────────────────────────
// MonkDB Workbench — Shared Dashboard Layout Constants
// Single source of truth — imported by both DashboardViewer and DashboardBuilder.
// ─────────────────────────────────────────────────────────────────────────────

/** Pixel height of each grid row unit. */
export const ROW_HEIGHT = 160;

/** Number of grid columns in the 12-column layout. */
export const COL_COUNT = 12;

/** Gap between widgets in pixels. */
export const GAP = 14;
